<Page x:Class="CafeManager.Pages.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:CafeManager.Pages"
        mc:Ignorable="d"
        Title="Login" Height="600" Width="800"
        Background="{StaticResource PrimaryBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Label Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="30" Foreground="White">Přihlášení</Label>
        <TextBox x:Name="emailInput" Style="{StaticResource TextBoxStyle}" Tag="Zadejte Email" Grid.Column="1" Grid.Row="2"/>
        <TextBox x:Name="password" Style="{StaticResource TextBoxStyle}" Tag="Zadejte Heslo" Grid.Column="1" Grid.Row="3"/>
        <Button Style="{StaticResource SubmitBtnStyle}" Grid.Column="1" Grid.Row="4" >Přihlásit se</Button>
        <Button Click="RegisterButton_Click" Style="{StaticResource SwitchButtonStyle}" Grid.Column="1" Grid.Row="5" >Vytvořit účet</Button>
    </Grid>
</Page>
