<Page x:Class="CafeManager.Pages.Menu"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:sb="clr-namespace:CafeManager.SideBar"
      Title="Menu"
      Background="{StaticResource PrimaryBackgroundBrush}">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/> 
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <sb:SideBar Grid.Column="0"/>
    
        <TextBlock Grid.Column="1" Text="Obsah stránky Menu" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Page>